<div class="card text-justify p-4">
  <div class="card-header">
    <h4 class="card-title">{{post.titulo}}</h4>
     <h6 class="card-subtitle">{{post.descricao}}</h6>   
         <p class="card-text fs-5"> data Publicação {{post.data}}</p>

  </div>
  <div class="card-body mt-3">
    <div class="card-text text-break fs-5">{{{markConteudo}}}</div>
  </div>
</div>